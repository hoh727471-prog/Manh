#include <iostream>
using namespace std;

class MaTran
{
private:
    // Thuộc tính của lớp MaTran
    int soHang;          // Số hàng của ma trận
    int soCot;           // Số cột của ma trận
    int a[100][100];     // Các phần tử của ma trận

public:
    // Hàm tạo mặc định
    MaTran()
    {
        soHang = 0;
        soCot = 0;
    }

    // Hàm tạo có tham số
    MaTran(int hang, int cot)
    {
        soHang = hang;
        soCot = cot;
    }

    // Hàm hủy
    ~MaTran()
    {
    }
};#include <iostream>
using namespace std;

class MaTran
{
private:
    // Thuộc tính của lớp MaTran
    int soHang;          // Số hàng của ma trận
    int soCot;           // Số cột của ma trận
    int a[100][100];     // Các phần tử của ma trận

public:
    // Hàm tạo mặc định
    MaTran()
    {
        soHang = 0;
        soCot = 0;
    }

    // Hàm tạo có tham số
    MaTran(int hang, int cot)
    {
        soHang = hang;
        soCot = cot;
    }

    // Hàm hủy
    ~MaTran()
    {
    }
};#include <iostream>
using namespace std;

class MaTran
{
private:
    // Thuộc tính của lớp MaTran
    int soHang;          // Số hàng của ma trận
    int soCot;           // Số cột của ma trận
    int a[100][100];     // Các phần tử của ma trận

public:
    // Hàm tạo mặc định
    MaTran()
    {
        soHang = 0;
        soCot = 0;
    }

    // Hàm tạo có tham số
    MaTran(int hang, int cot)
    {
        soHang = hang;
        soCot = cot;
    }

    // Hàm hủy
    ~MaTran()
    {
    }
};#include <iostream>
using namespace std;

class MaTran
{
private:
    // Thuộc tính của lớp MaTran
    int soHang;          // Số hàng của ma trận
    int soCot;           // Số cột của ma trận
    int a[100][100];     // Các phần tử của ma trận

public:
    // Hàm tạo mặc định
    MaTran()
    {
        soHang = 0;
        soCot = 0;
    }

    // Hàm tạo có tham số
    MaTran(int hang, int cot)
    {
        soHang = hang;
        soCot = cot;
    }

    // Hàm hủy
    ~MaTran()
    {
    }
};#include <iostream>
using namespace std;

class MaTran
{
private:
    // Thuộc tính của lớp MaTran
    int soHang;          // Số hàng của ma trận
    int soCot;           // Số cột của ma trận
    int a[100][100];     // Các phần tử của ma trận

public:
    // Hàm tạo mặc định
    MaTran()
    {
        soHang = 0;
        soCot = 0;
    }

    // Hàm tạo có tham số
    MaTran(int hang, int cot)
    {
        soHang = hang;
        soCot = cot;
    }

    // Hàm hủy
    ~MaTran()
    {
    }
};